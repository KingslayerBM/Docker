# HTTP
PORT=8081

# Switch repository adapter: "db" or "file"
REPO_ADAPTER=db

# Postgres (used when REPO_ADAPTER=db)
DB_HOST=db
DB_PORT=5432
DB_NAME=shop
DB_USER=shop
DB_PASS=shop

# CSV (used when REPO_ADAPTER=file)
CSV_PATH=/data/orders.csv
