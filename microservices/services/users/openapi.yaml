openapi: 3.0.3
info:
  title: Users Service
  version: "1.0.0"
paths:
  /users/{id}:
    get:
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      responses:
        "200":
          description: User
        "404":
          description: Not found
  /health:
    get:
      responses:
        "200":
          description: OK
